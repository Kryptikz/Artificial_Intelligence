package Test1;

import org.jblas.*;

public class Network {
	DoubleMatrix[] wMatrixArr;
	DoubleMatrix[] bMatrixArr;
	String activationFunc = "sigmoid";
	
	public Network(int[] layers) {
		wMatrixArr = new DoubleMatrix[layers.length-1];
		for(int i=0;i<layers.length-1;i++) {
			wMatrixArr[i] = new DoubleMatrix(layers[i+1],
		}
	}	
}